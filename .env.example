# GeoGuessr Verification API Configuration

# Server port (default: 8080)
PORT=8080

# GeoGuessr NCFA authentication token (REQUIRED)
# Get this from Chrome DevTools: Application > Cookies > _ncfa
GEOGUESSR_NCFA_TOKEN=your_ncfa_token_here

# Verification code expiry time in minutes (default: 5)
CODE_EXPIRY_MINUTES=5

# Rate limit per user per hour (default: 3)
RATE_LIMIT_PER_HOUR=3

# Allowed hosts for callback URLs (comma-separated, default: localhost,127.0.0.1,::1)
# For Docker: add your service names like "localhost,127.0.0.1,::1,api"
ALLOWED_CALLBACK_HOSTS=localhost,127.0.0.1,::1